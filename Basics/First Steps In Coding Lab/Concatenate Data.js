function givingInfo(input) {
    let name = input[0];
    let familyName = input[1];
    let age = input[2];
    let city = input [3];
    let givingInfo = "You are " + name + " " + familyName + ", a " + age + "-years old person from " + city + ".";
    console.log(givingInfo); 
}
