function matrix(num) {
    let array = [];
    for(let index = 0; index < num; index++) {
        array.push(num);
    }
    array = array.join(" ");

    for(let currentColumn = 1; currentColumn <= num; currentColumn++) {
    console.log(array)
        
    }
}
