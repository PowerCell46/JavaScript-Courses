function stringLength(firstStr, secondStr, thirdStr) {
    console.log(`${firstStr.length + secondStr.length + thirdStr.length}\n${Math.floor((firstStr.length + secondStr.length + thirdStr.length) / 3)}`);
}
